{
  "name": "prism",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PRISM — an on-device AI vision studio that runs entirely in your browser. Zero servers.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.7.1",
    "three": "^0.170.0"
  }
}
